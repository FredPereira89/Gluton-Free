# Names only. Real values live in .env.local (git-ignored), Vercel, and Trigger.dev Prod.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
# Session pooler URL (the direct host is IPv6-only)
SUPABASE_DB_URL=
TRIGGER_SECRET_KEY=
TRIGGER_PROJECT_REF=
ANTHROPIC_API_KEY=
DATAFORSEO_LOGIN=
DATAFORSEO_PASSWORD=
# The Supabase Auth user id (auth.users.id) of the single owner account.
# Create the user once in the Supabase dashboard, then copy its UUID here.
OWNER_USER_ID=
